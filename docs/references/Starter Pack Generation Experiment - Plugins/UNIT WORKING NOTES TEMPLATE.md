# Unit [Y] Working Notes — Cross-Module Continuity Tracker

This file is the cross-module "baton" for Unit [Y]. Update after each module's Task 4 completes. Every Task 1 chat should read this file to pick up context from prior modules.

<!-- 
HOW TO USE THIS TEMPLATE:

1. Copy this file into the unit folder as UNIT[Y]_WORKING_NOTES.md
2. Replace the module sections below with your unit's actual modules
3. Add module-specific tracker fields where needed (see examples below)
4. After each module's Task 4 completes, fill in that module's section
5. Every new Task 1 chat reads this file before starting

STANDARD FIELDS (include for every module):
- Status: Not started / In progress (Task N) / Complete
- Vocabulary introduced: term → interaction ID where formally introduced
- Bridge-to-next text: exact §1.9 Forward Bridge text
- Dimension values used: number ranges, problem types, etc.
- Toy configurations: Toy Name (Mode) — description
- Gate eval artifacts: file path for each completed gate's eval output (receipt chain — next Task verifies these exist before building on results)
- Open Author Flags: any ⚠️ flags affecting downstream modules

MODULE-SPECIFIC FIELDS (add when relevant):
- Notation variant used — for modules that introduce written methods (algorithms, expanded form)
- Error types surfaced — for error analysis modules
- Unknown representation — for units with progressive symbol scaffolding (?, □, letter)
- Division types covered — for division units (partitive/quotitive tracking)
- Diagnostic gate result — for modules with prerequisite gates
- Re-activation approach — for modules that resume a strand after a gap
- Schemas covered — for word problem modules (change, combine, compare)
- Zero patterns covered — for modules targeting subtraction across zeros
- "Add a zero" addressed? — for multiples-of-10 modules
- Commutativity ÷ test — for modules exploring property boundaries
- Decomposition strategies demonstrated — for area model / breaking-apart modules

Pick the fields that match the module's unique cross-module continuity concerns.
-->

---

## M1: [Module Title]

| Field | Value |
|-------|-------|
| **Status** | Not started |
| **Vocabulary introduced** | _[Update: term → interaction ID where formally introduced]_ |
| **Bridge-to-next text** | _[Paste exact §1.9 Forward Bridge text after Task 4]_ |
| **Dimension values used** | _[Number ranges, problem types, representation types]_ |
| **Toy configurations** | _[Toy Name (Mode) — what was on screen]_ |
| **Gate eval artifacts** | _[Gate 1: `path/to/report` · Gate 2: · Gate 3: · Gate 4: ]_ |
| **Open Author Flags** | _[Any ⚠️ flags that affect M2+]_ |

<!-- Add module-specific fields as needed. Example:
| **Notation variant used** | _[Which algorithm notation style]_ |
-->

---

## M2: [Module Title]

| Field | Value |
|-------|-------|
| **Status** | Not started |
| **Vocabulary introduced** | |
| **Bridge-to-next text** | |
| **Dimension values used** | |
| **Toy configurations** | |
| **Open Author Flags** | |

---

<!-- 
REPEAT for each module M3 through M[N].

For the FINAL module in the unit, change the Bridge-to-next field to:
| **Bridge-to-next text** | _[N/A — final module. Note: what does Unit [Y+1] expect students to bring?]_ |
-->

## M[N]: [Module Title]

| Field | Value |
|-------|-------|
| **Status** | Not started |
| **Vocabulary introduced** | |
| **Bridge-to-next text** | _[N/A — final module. Note: what does Unit [Y+1] expect students to bring?]_ |
| **Dimension values used** | |
| **Toy configurations** | |
| **Open Author Flags** | |
