title = "exitcheck"
type = "sequence"

# --- Section: transition_into_exit_check ---
[section_100_transition_into_exit_check]
template = "PLACEHOLDER"

[step_100_transition_into_exit_check]
dialogue = "You built equations with bags, rows, stacks, and groups. You found unknown numbers in different positions. Let's see what you know."
components = ["SceneComponent"]

# --- Section: build_complete_equation_variety_visual ---
[section_101_build_complete_equation_variety_visual]
template = "PLACEHOLDER"

[step_101_build_complete_equation_variety_visual]
dialogue = "Rows of pencils. Build the equation."
components = ["SceneComponent", "PromptComponent"]

# --- Section: solve_product_unknown_reversed_form ---
[section_102_solve_product_unknown_reversed_form]
template = "PLACEHOLDER"

[step_102_solve_product_unknown_reversed_form]
dialogue = "4 groups of 7 flowers. The equation is flipped. Total goes first. Find the total and place it."
components = ["SceneComponent", "PromptComponent"]

# --- Section: solve_1st_factor_unknown ---
[section_103_solve_1st_factor_unknown]
template = "PLACEHOLDER"

[step_103_solve_1st_factor_unknown]
dialogue = "How many stacks of 5 make 15? Pick the number."
components = ["SceneComponent", "PromptComponent"]

# --- Section: exit_check_closure ---
[section_104_exit_check_closure]
template = "PLACEHOLDER"

[step_104_exit_check_closure]
dialogue = "You're ready. Let's practice."
components = ["SceneComponent"]
