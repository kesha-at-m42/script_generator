title = "exitcheck"
type = "sequence"

# --- Section: transition_into_exit_check ---
[section_100_transition_into_exit_check]
template = "PLACEHOLDER"

[step_100_transition_into_exit_check]
dialogue = "You've built expressions from pictures and matched expressions to pictures. Let's see what you know."
components = ["SceneComponent"]

# --- Section: build_expression_boxes ---
[section_101_build_expression_boxes]
template = "PLACEHOLDER"

[step_101_build_expression_boxes]
dialogue = "Build the expression for these boxes."
components = ["SceneComponent", "PromptComponent"]

# --- Section: identify_visual_expression ---
[section_102_identify_visual_expression]
template = "PLACEHOLDER"

[step_102_identify_visual_expression]
dialogue = "Which picture matches this expression?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: build_expression_circles ---
[section_103_build_expression_circles]
template = "PLACEHOLDER"

[step_103_build_expression_circles]
dialogue = "Build the expression for the circles."
components = ["SceneComponent", "PromptComponent"]
