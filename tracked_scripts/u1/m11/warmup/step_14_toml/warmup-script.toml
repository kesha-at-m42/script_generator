title = "warmup"
type = "sequence"

# --- Section: opening_hook ---
[section_100_opening_hook]
template = "array_template"

[step_100_opening_hook]
dialogue = "Same items, two different pictures. What's going on here? Let's start with the one you already know."

# --- Section: equation_building_callback ---
[section_101_equation_building_callback]
template = "array_template"

[step_101_equation_building_callback]
dialogue = "Check out these bags. Build the equation."
components = ["PromptComponent"]

# --- Section: equal_groups_vs_rectangular ---
[section_102_equal_groups_vs_rectangular]
template = "array_template"

[step_102_equal_groups_vs_rectangular]
dialogue = "Let's compare these two pictures now. Both show 15 items: 3 groups of 5. But look at the one on the right. Something's different about how it's set up. What do you notice?"
components = ["PromptComponent"]

# --- Section: bridge_to_lesson ---
[section_103_bridge_to_lesson]
template = "array_template"

[step_103_bridge_to_lesson]
dialogue = "That lined-up arrangement? It has a name and there's a lot we can figure out about how it works. Let's dive in."
