title = "synthesis"
type = "sequence"

# --- Section: pattern_discovery_what_makes_groups ---
[section_100_pattern_discovery_what_makes_groups]
template = "PLACEHOLDER"

[step_100_pattern_discovery_what_makes_groups]
dialogue = "Here are four pictures. Three of them show equal groups. One does NOT. Which one is NOT equal groups?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: representation_transfer_same_multiplication_three ---
[section_101_representation_transfer_same_multiplication_three]
template = "PLACEHOLDER"

[step_101_representation_transfer_same_multiplication_three]
dialogue = "Three different pictures. Take a look at each one. What do all three pictures show?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: real_world_bridge_equal_groups ---
[section_102_real_world_bridge_equal_groups]
template = "PLACEHOLDER"

[step_102_real_world_bridge_equal_groups]
dialogue = "Equal groups aren't just in math problems. Here's a real situation."
components = ["SceneComponent"]

[step_103_real_world_bridge_equal_groups]
dialogue = "Can you build what the shelf looks like?"
components = ["PromptComponent"]

[step_104_real_world_bridge_equal_groups]
dialogue = "How many in each pack? Add the juice boxes."
components = ["PromptComponent"]

[step_105_real_world_bridge_equal_groups]
dialogue = "4 groups of 6. That's 4 × 6. Any time you see the same number in each container, packs, bags, boxes, cartons, that's equal groups. It's multiplication, even outside math class."

# --- Section: metacognitive_reflection_type_2_connection ---
[section_103_metacognitive_reflection_type_2_connection]
template = "PLACEHOLDER"

[step_106_metacognitive_reflection_type_2_connection]
dialogue = "Here's something to think about. Today's work connects to something you've been doing for a while. Did you notice a connection?"
components = ["SceneComponent", "PromptComponent"]
