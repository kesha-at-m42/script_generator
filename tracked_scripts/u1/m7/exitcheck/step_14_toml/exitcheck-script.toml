title = "exitcheck"
type = "sequence"

# --- Section: identify_groups_items_identify ---
[section_100_identify_groups_items_identify]
template = "PLACEHOLDER"

[step_100_identify_groups_items_identify]
dialogue = "Look at these boxes. Tell me what you see."
components = ["SceneComponent"]

[step_101_identify_groups_items_identify]
dialogue = "How many groups? How many in each group?"
components = ["PromptComponent"]

[step_102_identify_groups_items_identify]
dialogue = "Now how many in each group?"
components = ["PromptComponent"]

# --- Section: build_equal_groups_create ---
[section_101_build_equal_groups_create]
template = "PLACEHOLDER"

[step_103_build_equal_groups_create]
dialogue = "Build this one. Show me 2 groups of 3."
components = ["SceneComponent", "PromptComponent"]

[step_104_build_equal_groups_create]
dialogue = "How many in each group? Add the items."
components = ["PromptComponent"]

# --- Section: select_expression_identify ---
[section_102_select_expression_identify]
template = "PLACEHOLDER"

[step_105_select_expression_identify]
dialogue = "Alright, circles. Figure out the groups, then pick the expression that matches."
components = ["SceneComponent", "PromptComponent"]
