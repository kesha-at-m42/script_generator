title = "warmup"
type = "sequence"

# --- Section: graph_reading_quick_success ---
[section_100_graph_reading_quick_success]
template = "PLACEHOLDER"

[step_100_graph_reading_quick_success]
dialogue = "You know how to read these. Look. 4 symbols. Each one means 5 books. How many books altogether?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: grouping_animation_reveal ---
[section_101_grouping_animation_reveal]
template = "PLACEHOLDER"

[step_101_grouping_animation_reveal]
dialogue = "Wait. Look at what those symbols were hiding."
components = ["SceneComponent"]

[step_102_grouping_animation_reveal]
dialogue = "4 symbols. Each symbol held 5 items. That's 4 groups of 5. Look familiar? This is called multiplication. You've been doing multiplication this whole time."
components = ["PromptComponent"]

# --- Section: transfer_bags_groups_beyond_graphs ---
[section_102_transfer_bags_groups_beyond_graphs]
template = "PLACEHOLDER"

[step_103_transfer_bags_groups_beyond_graphs]
dialogue = "Here's a different picture. No graph. Just bags."
components = ["SceneComponent", "PromptComponent"]

# --- Section: bridge_lesson ---
[section_103_bridge_lesson]
template = "PLACEHOLDER"

[step_104_bridge_lesson]
dialogue = "You found equal groups in graphs. You found them in bags. There's a whole lot more hiding. Let's find them."
components = ["SceneComponent"]
