title = "warmup"
type = "sequence"

# --- Section: graph_reading_with_scale_2 ---
[section_100_graph_reading_with_scale_2]
template = "PLACEHOLDER"

[step_100_graph_reading_with_scale_2]
dialogue = "Let's warm up your graph skills. Here's a bar graph. Check the scale."
components = ["SceneComponent", "PromptComponent"]

# --- Section: comparison_question_single_step ---
[section_101_comparison_question_single_step]
template = "PLACEHOLDER"

[step_101_comparison_question_single_step]
dialogue = "Now a question about this data. How many more students chose soccer than tennis?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: quick_scale_check_lesson_context ---
[section_102_quick_scale_check_lesson_context]
template = "PLACEHOLDER"

[step_102_quick_scale_check_lesson_context]
dialogue = "Different graph, different scale. Quick check."
components = ["SceneComponent", "PromptComponent"]

# --- Section: bridge_lesson ---
[section_103_bridge_lesson]
template = "PLACEHOLDER"

[step_103_bridge_lesson]
dialogue = "You've got graph reading down. You've got comparison down. But look at THIS question."
components = ["SceneComponent"]

[step_104_bridge_lesson]
dialogue = "How many more students chose pizza than tacos and salad together?"

[step_105_bridge_lesson]
dialogue = "See that word 'together'? This question needs more than one step. We'll come back to this question in the lesson, and you'll be able to answer it."
