title = "exitcheck"
type = "sequence"

# --- Section: transition_into_exit_check ---
[section_100_transition_into_exit_check]
template = "PLACEHOLDER"

[step_100_transition_into_exit_check]
dialogue = "You made bar graphs with scale of 10. You read values at the lines and between the lines. Let's see what you know."
components = ["SceneComponent"]

# --- Section: read_value_at_axis_line ---
[section_101_read_value_at_axis_line]
template = "PLACEHOLDER"

[step_101_read_value_at_axis_line]
dialogue = "Look at the Fiction bar. What value does the Fiction bar show?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: read_value_between_lines_interpolation ---
[section_102_read_value_between_lines_interpolation]
template = "PLACEHOLDER"

[step_102_read_value_between_lines_interpolation]
dialogue = "Look at Team A's bar. It ends between the lines. The bar is between 30 and 40. What value does this show?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: create_bar_graph_with_mixed ---
[section_103_create_bar_graph_with_mixed]
template = "PLACEHOLDER"

[step_103_create_bar_graph_with_mixed]
dialogue = "Create the bar graph. Some values are at the lines, some are between."
components = ["SceneComponent", "PromptComponent"]

# --- Section: comparison_problem_compare ---
[section_104_comparison_problem_compare]
template = "PLACEHOLDER"

[step_104_comparison_problem_compare]
dialogue = "Use the graph you made. How many MORE art supplies does Glue Sticks have than Markers?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: exit_check_closure ---
[section_105_exit_check_closure]
template = "PLACEHOLDER"

[step_105_exit_check_closure]
dialogue = "You can read bar graphs with a scale of 10, even values between the lines. You're ready to practice."
components = ["SceneComponent"]
