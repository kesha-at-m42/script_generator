title = "exitcheck"
type = "sequence"

# --- Section: transition_into_exit_check ---
[section_100_transition_into_exit_check]
template = "PLACEHOLDER"

[step_100_transition_into_exit_check]
dialogue = "You learned to choose scales. You checked if the biggest number fits. You checked the last digits. Let's see what you know."
components = ["SceneComponent"]

# --- Section: scale_too_small_identify ---
[section_101_scale_too_small_identify]
template = "PLACEHOLDER"

[step_101_scale_too_small_identify]
dialogue = "Four friends collected toys for a donation drive. Ben collected the most with 70 toys. Which scale works best for this data?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: non_multiples_need_scale_2 ---
[section_102_non_multiples_need_scale_2]
template = "PLACEHOLDER"

[step_102_non_multiples_need_scale_2]
dialogue = "These students tracked how many minutes they read this week. Check the numbers: 23, 31, 9, 16. Which scale will show ALL these values exactly?"
components = ["SceneComponent", "PromptComponent"]

# --- Section: select_appropriate_scale_compare ---
[section_103_select_appropriate_scale_compare]
template = "PLACEHOLDER"

[step_103_select_appropriate_scale_compare]
dialogue = "All these numbers end in 0. Think about the biggest number."
components = ["SceneComponent", "PromptComponent"]

# --- Section: exit_check_closure ---
[section_104_exit_check_closure]
template = "PLACEHOLDER"

[step_104_exit_check_closure]
dialogue = "You're ready to practice."
components = ["SceneComponent"]
